#Runtime - needed to run the application
streamlit==1.59.0
chromadb==1.5.9
groq==1.5.0
pillow==12.1.0

#Deployment only - chromadb needs sqlite3 >= 3.35 and some hosts ship an older one. linux only, there is no Windows wheel
pysqlite3-binary; sys_platform == "linux"

#Knowledge base construction-build/ only
python-docx==1.2.0
python-pptx==1.0.2
pypdf==6.14.2
lxml==6.1.1

#Evaluation - evaluation/ and tests/ only
openpyxl==3.1.5
pytest==9.1.1
